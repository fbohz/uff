<%= form_with model: wall, local:true do |f| %>

    <div class="field">
    <h4 class="title is-4">Wall Artist(s):</h5>
        <%= f.text_field :artist_name, :class => "control"%>
    </div><br>
    <!-- need add new artist button, maybe break up this form? --> 
        <!-- this activates if 2 artists change according. --> 
         <div class="field">
         <div class="control">
            <%= f.label :two_plus_artists_contribution_details, size: 50 %>
            <%= f.text_field :artists_contribution_details %>
         </div></div>    
        <!-- above mockup only --> 
    <div class="field">
    <h4 class="title is-4">Wall Location:</h5>
        <%= f.text_field :wall_location, :class => "control"%>
    </div><br>

    <div class="field">
    <h4 class="title is-4">Wall Details:</h5>
        <div class="field">
         <div class="control">
            <%= f.label :date_done %>
            <%= f.date_select :date_done %>
         </div></div>
         <div class="field">
         <div class="control">
            <%= f.label :address, size: 50 %>
            <%= f.text_field :address %>
         </div></div>
         <div class="field">
         <div class="control">
            <%= f.label :description %>
            <%= f.text_area :description, cols: 50, rows: 6  %>
         </div></div>
        <div class="field">
         <!-- need implement add file later--> 
         <div class="control">
            <%= f.label :image %>
            <%= f.file_field :image %>
         </div></div>         

    <div class="field">
        <%= f.label :tags %>
        <%= f.text_field :wall_tags, :class => "control"%>
    </div><br>
        <!-- need add new tag button, maybe break up this form? --> 
    </div><br>
    <div class="buttons">
    <%= f.submit "Add", { class: 'button is-danger' } %>
    </div>

<% end %>